/**
 * @file src/utils/introFonts.js
 * Font stacks of the virtualization intro (supercomputer console, Superscan
 * pop-up). Kept apart from the site fonts in `fonts.js` so the intro can evolve
 * on its own. Orbitron is loaded from Google Fonts in `App.css`.
 * @module introFonts
 */

/** Console text: HUD, module labels, logs and data readouts. */
export const FONT_MONO = "'Courier New', monospace";

/** Titles, timer and the "Click to enter Lyoko" prompt. */
export const FONT_DISPLAY = "'Orbitron', 'Courier New', monospace";
